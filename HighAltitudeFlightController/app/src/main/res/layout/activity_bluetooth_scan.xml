<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark">
    <ListView
        android:id="@+id/scan_listview"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_above="@+id/txt_hint"
        android:layout_alignParentTop="true">

    </ListView>
    <TextView
        android:id="@+id/txt_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Search for BLE modules ----->"
        android:textSize="16sp"
        android:textColor="@color/pearWhite"
        android:layout_marginBottom="15dp"
        android:layout_alignBottom="@+id/scan_floating_button"
        android:layout_centerHorizontal="true" />


    <android.support.design.widget.FloatingActionButton
        android:id="@+id/scan_floating_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        app:backgroundTint="@color/colorAccent"
        android:layout_marginBottom="6dp"
        android:layout_marginRight="6dp"
        android:src="@android:drawable/ic_menu_search"
        app:elevation="6dp"
        android:scaleType="center"
        android:layout_gravity ="end|bottom"
        app:pressedTranslationZ="12dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>